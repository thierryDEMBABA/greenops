name: CI - Service calcul

on:
  push:
    branches: [ main ]
    paths:
      - 'service-calcul/**' # Déclenchement granulaire

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code du dépôt
        uses: actions/checkout@v4

      - name: Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: service-calcul/requirements.txt

      - name: Installer les dépendances et valider la syntaxe
        run: |
          python -m pip install --upgrade pip
          pip install flake8
          if [ -f service-calcul/requirements.txt ]; then pip install -r service-calcul/requirements.txt; fi
          flake8 service-calcul --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Connexion sécurisée au Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Configurer Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build et Push de l'image
        uses: docker/build-push-action@v5
        with:
          context: ./service-calcul
          file: ./service-calcul/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/greenops_service_calcul:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/greenops_service_calcul:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max